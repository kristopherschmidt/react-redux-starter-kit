import makeRoutes from './routes'
import reducer from './reducer'
import AppContainer from './components/AppContainer'
import configureStore from './configureStoreUtil'

export default { makeRoutes, reducer, AppContainer, configureStore }
